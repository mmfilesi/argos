'use strict';

/**
 * @namespace viewstUtils
 * @description utilities views (screens)
 */

const viewstUtils = (function() {

  const module    = {};
  const self      = module;

  module.renderTemplate = function(errorMsg) {
    msg.showErrorServer(errorMsg);
  };

  return {
    renderTemplate: module.renderTemplate
  };

})();
